# 词星球发布验收 · 2026-09-05

本记录区分已实际检查的结果与尚未验证的边界，不是“100分”或企业服务保证。

## 本次页面与流程修复

- 孩子页面移除来源核验、接口配置、缓存及保存机制等实现叙述。教材版本、语音性质、录音隐私与配图许可保留在折叠的“家长查看”及工具箱“家长工具”，不掩盖旧版教材和音标缺项。
- 首页突出当前课本、单词、图片、音标与开始学习；缩小过大的标题与留白，正常预加载不再占据提示区。只有实际点击后才提示准备/播放，英美两种声音在浏览器中实际播放完成。
- 词表恢复书签单元和词；跨单元搜索隐藏无关大卡片；词表和练习失败可原地重试。拼写有独立中文提示，输入为空不可提交；保存失败提醒跨自动切词保留。
- 返回练习对应词表、游戏与默写默认跟随书签；泡泡、拼写、翻牌结束后可返回游戏中心再开始。浏览器实际验证泡泡 2 轮完成和重新开局状态清空。
- 家长同步区跨路由保留配对码；备份控件进入工具箱时重新读取，导出前再次取最新记录。读取失败不下载旧备份。回归覆盖工具箱→拼写→工具箱后新增记录可见。
- 390×844 实测修复底部前后按钮覆盖拼写输入框，输入和检查按钮完整可见；768×1024 跟读及 1280×800 学习页面无横向溢出。
- 四上 Unit 6 的 22 题预览为 2 页题目、1 页答案，默认教材与单元正确。系统打印与麦克风真机边界仍见下文。
- 本次通过浏览器操作验证关键流程，未运行 Playwright CLI；相应 E2E 用例已同步新文案和 1195 词计数。验收在本地预览产生的 3 条作答记录已清理，未触碰线上用户记录。

## 内容与发音

- 八册共1195词：212 / 197 / 165 / 135 / 152 / 149 / 81 / 104。
- 四年级原上册入口以用户照片第82–85页165词替换，Unit 1–6为31 / 40 / 27 / 19 / 26 / 22词；首词sport，末词aunt。没有新增重复册次。
- 照片未展示版权页；不能确认出版年份与印次。四下、五六年级仍是旧版Module来源，不宣称全套最新。
- 四上164/165项具备可追溯英美音标；because of sb / sth为教材占位表达，尚无整条已核对音标。
- 963个不重复词的双口音加过去式read专用一对，共1928个MP3。1195词的2390条发音解析均有对应文件，FFmpeg全量解码失败0、静音文件0。生成语音明确标注为合成音，不冒充教材原声。
- 裁去音源前静音后，声音起点中位数从215ms变为37ms、P95从248ms变为49ms；这不是点击到扬声器响起的端到端延迟。保留轻辅音和词尾，完整处理波形相关系数最低0.98498。详见public/audio/quality-summary.json。
- 四上94/165项有来源配图，包含41项本地OpenMoji，其余来源图仍需网络。未逐张目视核对所有远程图；失败时显示明确词义提示，不伪装成精确插图。测验不展示可能泄露英文的远程图。

## 学习流程和真实性

- 每组最多8词：学习、认词、拼写、听写、跟读。学习/跟读显示英文与英美音标；测试模式隐藏答案。
- 正确后自动进入下一词，支持上一词、下一词、返回、书签续学；本组完成后可重练错词或进入下一组。
- 浏览和即时重复不算长期掌握；复习间隔1/3/7/14/30天，错词重新巩固。借鉴间隔复习和短组学练思路，不宣称实现Anki FSRS。
- 游戏结果只来自实际作答。持久保存失败有明确提示，不再无条件显示“已保存”。
- 跟读是本地录音和回放，录音不上传，不编造AI评分。真机麦克风权限与音质尚未逐型号验收。

## 自动检查

- Vitest：40个测试文件、256项测试，覆盖真实词表替换、分组/错词/续学、游戏、发音异常、录音状态、打印、存储、备份与安全边界。
- Node资源导入测试4项：音标口音解析、图片目录一致性、SVG安全和作者归属。
- TypeScript检查与Vite/PWA生产构建。
- 主入口JS约346KB（gzip约106KB），练习、教材、游戏、打印和同步按页面拆包；当前和下一词双口音预加载、失败可重试。首次未缓存音频仍需网络。
- GitHub发布流程使用Node24，并在测试、资源检查和构建通过后发布。

## 前一轮浏览器实测（保留记录）

- 390×844手机：首屏可见英文和英美发音按钮；实际sport↔jump前后导航、拼写sport正确后进入jump；无横向溢出。
- 768×1024平板：品牌区与吉祥物不再重叠，六个导航入口保持单行，内容区正常展开。
- 1280×900电脑：宽屏学习/工具区不再被多余右栏挤压；四上Unit2生成40题，4页题目（12/12/12/4）及2页独立答案（30/10）。配图54px且正常加载，无点状缩小或横向溢出。
- 英美播放按钮实际进入对应“正在播放”状态。没有进行人工逐条听音，未覆盖全部微信内置浏览器或离线首次访问。
- 打印分页、答案隔离和打印调用有自动测试；浏览器预览布局已检查，尚未连接实体打印机或逐浏览器验证系统打印对话框。
- 本轮预览产生的一条合成测试作答已通过界面清除，未清除线上用户记录。

## 真实后端

- HTTPS API： https://word-planet-sync.zhanyiqing514.chatgpt.site
- 用户明确同意并操作后才备份；AES-GCM加密，认证密钥独立派生，服务端只保存密文。不收集姓名、邮箱或录音，不在URL中传递配对码。
- 线上实际完成设备A创建、B读取更新、A读回、409旧版本冲突、错误密钥404、删除204和删除后404。使用临时合成记录，测试备份已删除；脚本为scripts/verify-live-sync.mjs。
- 配对码默认不永久保存在设备；切换页面仍保留本次输入，不会意外丢失。跨设备为手动合并，不是实时同步。
- 180天未更新失效；128个备份的容量和请求限额已实现。免费服务不提供企业SLA，达到限额会明确提示并保留本地导出路径。

## 清理和发布边界

- 清除138个不再被当前八册引用的旧音频、旧通用示例绘图代码及3张过期验收截图；历史受版本控制的文件可以恢复。
- 只修改独立WordPlanetWeb项目并发布到原word-planet网址；不替换RuleQuant项目或既有桌面版。
- 页面更新不等于所有教材版本、每一张图、所有手机与网络都已核验。本文件明确保留这些边界。
